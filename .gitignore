.claude/worktrees/
.wrangler/

# Phase B file-lock artifacts
*.lock

# Playwright (Phase B V2 UI §S.4)
node_modules/
playwright-report/
test-results/
.playwright/

# Data files symlinked from ~/norris-agent/data for LAN preview only
# (Real files live in the agent repo; symlinks are for local dev, not deploy.)
data/customer_registry.json

# V2.3 build state files (per-session, not source)
.v2.3-*

# Pre-build backups (the 'pre_<date>' suffix is the convention)
*.bak_pre_*
*.json.bak_pre_*
